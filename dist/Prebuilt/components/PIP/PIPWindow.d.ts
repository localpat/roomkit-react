import React from "react";
type PIPWindowProps = {
    pipWindow: Window;
    children: React.ReactNode;
};
export declare const PIPWindow: ({ pipWindow, children }: PIPWindowProps) => any;
export {};
