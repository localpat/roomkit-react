import React from "react";
export declare const VideoProgress: ({ seekProgress, setSeekProgress, }: {
    seekProgress: boolean;
    setSeekProgress: (value: boolean) => void;
}) => React.JSX.Element | null;
